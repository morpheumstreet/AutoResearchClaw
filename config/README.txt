Place your ResearchClaw YAML here as config.yaml (e.g. copy ../config.researchclaw.example.yaml).
This directory is mounted at /workspace/user-config so Docker never creates a mistaken config.yaml folder.
